config:
  target: "http://httpbin.org/"  
  phases:
    - duration: 1
      arrivalRate: 2
      name: Goit Performance Test

scenarios:
  - name: Put Request
    flow:
      - put:
          url: "{{target}}put"